@model IEnumerable<Baby.Models.Donation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Currency.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Donor.Surname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Need.Caption)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Organization.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fee)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Currency.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Donor.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Need.Caption)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Organization.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fee)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonationId })
        </td>
    </tr>
}

</table>
